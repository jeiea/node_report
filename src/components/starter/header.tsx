import { component$ } from "@builder.io/qwik";
import { css } from "~/styled-system/css";

export default component$(() => {
  return (
    <header class={css({})}>
      <div class={css({})}></div>
    </header>
  );
});
